from main import creditCardDetails

def test_creditcard () :
    pay(name: string, cardNumber: string cvv: number, expiryDate: string): boolean {
        if (name == "Pris" && cvv == 123 && expDate == "2024-11" && cardNumber == "1234567890123450") {
            return true
        } else {
            return false
        }
    }